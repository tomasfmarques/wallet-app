---
description: Load current project state at the start of a session
---

Get me up to speed on Wallet360 without dumping the whole repo:

1. Read `docs/STATE.md` and summarise: current status, the top 3 next steps, and any open threads.
2. Run `git log --oneline -10` and `git status` to see what changed most recently and whether the tree is clean.
3. If I name a module (e.g. "budget"), also read `docs/decisions/<module>.md`. Otherwise do NOT read the decision files yet.

Keep it to a short briefing. End by asking what I want to work on.
